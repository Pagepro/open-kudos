{
  "name": "open-kudos",
  "version": "0.0.0-semantically-released",
  "license": "MIT",
  "scripts": {
    "postinstall": "sh build.sh",
    "watch": "cd backend && yarn run watch",
    "git-cz": "cd backend && yarn run git-cz"
  },
  "engines": {
    "yarn": "1.x"
  }
}
