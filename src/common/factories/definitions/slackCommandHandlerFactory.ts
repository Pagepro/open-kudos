export enum SlackCommandType {
  give = 'give',
  help = 'help',
  balance = 'balance',
  gifts = 'gifts'
}

export enum SlackResponseType {
  hidden = 'hidden',
  general = 'general',
  standard = 'standard'
}

export enum SlackActionsCallbacks {
  buyGift = 'buyGift'
}
